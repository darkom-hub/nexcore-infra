---
postfix_packages:
  - postfix

postfix_service: postfix

mail_domain: nexcore.local

mail_hostname: servera.nexcore.local

smtp_port: 25

mail_relay_networks:
  - 127.0.0.0/8

dovecot_packages:
  - dovecot

dovecot_service: dovecot

imap_port: 143
