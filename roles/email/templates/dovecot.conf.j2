protocols = imap

listen = *

mail_location = maildir:~/Maildir

auth_mechanisms = plain login

passdb {
  driver = pam
}

userdb {
  driver = passwd
}

service imap-login {
  inet_listener imap {
    port = {{ imap_port }}
  }
}
